# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore build and test binaries.
bin/
# Never copy local node_modules into images; npm ci handles installation inside the container.
ui/node_modules/
